/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "media.guardianapis.com",
      "uploads.guardiancdn.com",
      "media.guim.co.uk",
    ],
  },
};

export default nextConfig;
